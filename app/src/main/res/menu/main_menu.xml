<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/filter_date"
        android:title="@string/filter_by_date"
        android:contentDescription="@string/filter_by_date"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/filter_room"
        android:title="@string/filter_by_room"
        android:contentDescription="@string/filter_by_room"
        app:showAsAction="never">
    </item>

    <item
        android:id="@+id/filter_clear"
        android:contentDescription="@string/clear_all_filter"
        android:title="@string/clear_all_filter"
        app:showAsAction="never">
    </item>

</menu>